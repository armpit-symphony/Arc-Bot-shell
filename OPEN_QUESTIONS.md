# Open Questions

Date: 2026-06-18
Status: Foundation planning

1. What is the minimal safe policy envelope required before any preview-to-execution transition?
2. Which tenant isolation and data-segregation mechanism is required for initial customer proof deployment?
3. Which approval token format should be first-class in shell-to-runtime transitions?
4. Which evidence fields must be required for each blocked action (`policy_ref`, `evidence_ref`, `runbook_ref` minimum)?
5. How should worker capability scope be normalized across mini-PC hardware profiles?
6. What minimal subset of connector telemetry can be exposed before connector actions are unlocked?
7. What is the exact migration matrix for converting read-only projections to signed runtime envelopes?
